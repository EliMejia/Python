#Temporizador

"""
Se puede agregar un temporizador que permita realizar una funcion cada 
cierto tiempo, se debe tener en cuenta que se maneja en milisegundos
"""

#Ejemplo:

import simplegui

def tick()
  name="ELI"
  print name+"MEJIA"
                            #(Cada cuando se ejecuta, nombre de la funcion que ejecuta)
 timer=simplegui.create_timer(100,tick)
 time.start() #Da inicio al temporizador
 time.stop()  #Detiene el temporizador
